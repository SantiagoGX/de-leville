
:root {
  --font-main: var(--font-body-family, 'Futura PT', sans-serif);
  --font-nunito: var(--font-body-family, 'Futura PT', sans-serif);
  font-family: var(--font-main);
}

html,
body,
* {
  font-family: var(--font-body-family, 'Futura PT', sans-serif);
  letter-spacing: -0.04em;
}
